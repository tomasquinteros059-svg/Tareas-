{
  "name": "@tareas/movil",
  "version": "0.1.0",
  "private": true,
  "description": "Envoltorio Android de la app: la misma app instalable, empaquetada como APK.",
  "scripts": {
    "preparar": "node ../web/construir.mjs && cap sync android",
    "android": "cap add android || true",
    "apk": "npm run android && npm run preparar && cd android && ./gradlew assembleDebug",
    "abrir": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1"
  }
}
